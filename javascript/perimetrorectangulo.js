const width = Number(prompt("Ingrese el ancho del rectángulo"));
const height = Number(prompt("Ingrese la altura del rectángulo"));

const perimeter = (width + height) * 2;

alert(`El perímetro del rectángulo es ${perimeter}.`);