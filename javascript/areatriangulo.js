const base = Number(prompt("Ingrese el valor de la base"));
const height = Number(prompt("Ingrese el valor de la altura"));

const area = (base * height) / 2;

alert(`El área del triángulo es ${area}.`);